# Raw MCP data (large, ephemeral — regenerated each cycle)
data/raw-*.json
data/partials/
data/campaign-discovery-plan.json
data/campaign-reconciliation.json
data/live-campaigns.json
data/pull-plan.json

# Compressed snapshot (regenerated each cycle)
data/snapshot.json

# Python
__pycache__/
*.pyc
.venv/

# Secrets
.env

# OS
.DS_Store
.mcp.json
